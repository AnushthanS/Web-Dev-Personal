let person = {
    name: 'Anushthan',
    age: 20
}; // this is an object, can be thought of something similar to a struct

console.log(person);


let selectedColors = ['red', 'green', 'blue', 333]; // can store any data type, no constraints
console.log(selectedColors);